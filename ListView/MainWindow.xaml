<Window x:Class="ListView.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ListView"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        FontSize="18">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="35"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition /> 
            <ColumnDefinition /> 
        </Grid.ColumnDefinitions>

        <TextBox Name="txtEntry" Height="35"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions />
        </Grid>

            <Button Grid.Column="1" Width="100" Name="dtnAdd" Content="Add"    HorizontalAlignment="Left" Margin="5, 0, 0, 0" Click="dtnAdd_Click"/>
        <Button Grid.Column="1" Width="100" Name="dtnDelete" Content="Delete" HorizontalAlignment="Left" Margin="105, 0, 0 ,0" Click="dtnDelete_Click" />
        <Button Grid.Column="1" Width="100" Name="dtnCleare" Content="Clear"  HorizontalAlignment="Left" Margin="205, 0, 0 ,0" Click="dtnCleare_Click" />

        <ListView Name="lvEntries" Grid.Row="1" Grid.ColumnSpan="2" />

    </Grid>
</Window>
